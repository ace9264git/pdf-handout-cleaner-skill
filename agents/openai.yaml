interface:
  display_name: "PDF Center Watermark Remover"
  short_description: "Remove center watermarks at near-original size."
  default_prompt: "Use $pdf-handout-cleaner to remove the center light-gray watermark while preserving resolution and keeping file size close to the original."

policy:
  allow_implicit_invocation: true
