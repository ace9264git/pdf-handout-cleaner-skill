interface:
  display_name: "PDF Center Watermark Remover"
  short_description: "Remove center gray watermarks without downsampling."
  default_prompt: "Use $pdf-handout-cleaner to remove the center light-gray watermark without compressing or downsampling the PDF."

policy:
  allow_implicit_invocation: true
