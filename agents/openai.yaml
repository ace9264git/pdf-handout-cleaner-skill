interface:
  display_name: "PDF Handout Cleaner"
  short_description: "Compress PDFs and remove center gray watermarks."
  default_prompt: "Use $pdf-handout-cleaner to compress these teaching PDFs at 300 DPI and remove center light-gray watermarks."

policy:
  allow_implicit_invocation: true
